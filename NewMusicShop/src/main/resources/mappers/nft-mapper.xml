<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "httpS://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="nftMapper">

  <!-- 나의 NFT 목록 조회 -->
  <select id="selectNFTsByUser" resultType="nft">
    SELECT NFT_ID, USER_ID, MUSIC_NO, MINT_HASH, MINT_DATE
    FROM NFT_OWNERSHIP
    WHERE USER_ID = #{userId}
    ORDER BY MINT_DATE DESC
  </select>

  <!-- NFT 단건 상세 조회 -->
  <select id="selectNFTById" parameterType="String" resultType="nft">
    SELECT NFT_ID, USER_ID, MUSIC_NO, MINT_HASH, MINT_DATE
    FROM NFT_OWNERSHIP
    WHERE NFT_ID = #{nftId}
  </select>

  <!-- NFT 민팅 정보 등록 -->
  <insert id="insertNFTRecord" parameterType="nft">
    INSERT INTO NFT_OWNERSHIP (NFT_ID, USER_ID, MUSIC_NO, MINT_HASH)
    VALUES (SEQ_NFT_ID.NEXTVAL, #{userId}, #{musicNo}, #{mintHash})
  </insert>

<select id="selectAllNFTs" resultType="nft">
  SELECT NFT_ID, USER_ID, MUSIC_NO, MINT_HASH, MINT_DATE
  FROM NFT_OWNERSHIP
  ORDER BY MINT_DATE DESC
</select>



</mapper>
